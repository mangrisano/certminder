[build-system]
requires = ["setuptools>=77.0"]
build-backend = "setuptools.build_meta"

[project]
name = "certminder"
version = "0.9.0"
description = "Continuous TLS certificate monitoring and alerting, powered by certinspect."
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
license-files = ["LICENSE"]
authors = [{ name = "Michele Angrisano", email = "michele.angrisano@gmail.com" }]
keywords = ["tls", "ssl", "x509", "certificate", "monitoring", "alerting", "pki"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Console",
    "Intended Audience :: System Administrators",
    "Programming Language :: Python :: 3",
    "Topic :: System :: Monitoring",
    "Topic :: Security",
]
dependencies = [
    "certinspect>=1.9.1",
    "PyYAML>=6.0",
]

[project.optional-dependencies]
dev = ["pytest>=8.0", "ruff>=0.5"]

[project.urls]
Homepage = "https://github.com/mangrisano/certminder"
Repository = "https://github.com/mangrisano/certminder"
Issues = "https://github.com/mangrisano/certminder/issues"

[project.scripts]
certminder = "certminder.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
line-length = 88
src = ["src", "tests"]

[tool.ruff.lint]
# Pin the classic default rule set (pyflakes + a subset of pycodestyle) so
# linting stays reproducible no matter which ruff version CI installs: a newer
# ruff that enables extra rule families by default must not spontaneously break
# the build. Opt into more rules deliberately, not by tool drift.
select = ["E4", "E7", "E9", "F"]

[tool.pytest.ini_options]
testpaths = ["tests"]
