# Trigger certminder.service twice a day, with a small random delay to avoid a
# thundering herd against the monitored endpoints. Persistent=true catches up
# on a missed run after downtime.

[Unit]
Description=Run certminder periodically
Documentation=https://github.com/mangrisano/certminder

[Timer]
OnCalendar=*-*-* 06,18:00:00
RandomizedDelaySec=300
Persistent=true

[Install]
WantedBy=timers.target
