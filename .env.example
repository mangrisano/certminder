# Copy to .env (next to certminder.yml) and fill in real values. .env is
# gitignored — never commit it. Referenced from certminder.example.yml via
# ${VAR}; see "Using environment variables in the file" in README.md.

SLACK_WEBHOOK_URL=https://hooks.slack.com/services/T00/B00/xxxx
WEBHOOK_TOKEN=abc123
SMTP_PASSWORD=SuperSecret
